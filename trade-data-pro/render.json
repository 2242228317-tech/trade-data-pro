{
  "name": "trade-data-pro-deployment",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
